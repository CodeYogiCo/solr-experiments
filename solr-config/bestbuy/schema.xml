<?xml version="1.0" encoding="UTF-8" ?>
<schema name="bestbuy" version="1.6">

  <!-- ── Field types ─────────────────────────────────────────────────────── -->

  <fieldType name="string"  class="solr.StrField"  sortMissingLast="true" docValues="true"/>
  <fieldType name="strings" class="solr.StrField"  sortMissingLast="true" multiValued="true" docValues="true"/>
  <fieldType name="boolean" class="solr.BoolField" sortMissingLast="true"/>
  <fieldType name="int"     class="solr.IntPointField"    docValues="true"/>
  <fieldType name="long"    class="solr.LongPointField"   docValues="true"/>
  <fieldType name="float"   class="solr.FloatPointField"  docValues="true"/>
  <fieldType name="double"  class="solr.DoublePointField" docValues="true"/>
  <fieldType name="date"    class="solr.DatePointField"   docValues="true"/>
  <fieldType name="location" class="solr.LatLonPointSpatialField" docValues="true"/>

  <!-- Full-text with English stemming and stop words -->
  <fieldType name="text_en" class="solr.TextField" positionIncrementGap="100" autoGeneratePhraseQueries="true">
    <analyzer type="index">
      <tokenizer class="solr.WhitespaceTokenizerFactory"/>
      <filter class="solr.StopFilterFactory" ignoreCase="true" words="stopwords.txt"/>
      <filter class="solr.LowerCaseFilterFactory"/>
      <filter class="solr.EnglishPossessiveFilterFactory"/>
      <filter class="solr.PorterStemFilterFactory"/>
    </analyzer>
    <analyzer type="query">
      <tokenizer class="solr.WhitespaceTokenizerFactory"/>
      <filter class="solr.SynonymGraphFilterFactory" synonyms="synonyms.txt" ignoreCase="true" expand="true"/>
      <filter class="solr.StopFilterFactory" ignoreCase="true" words="stopwords.txt"/>
      <filter class="solr.LowerCaseFilterFactory"/>
      <filter class="solr.EnglishPossessiveFilterFactory"/>
      <filter class="solr.PorterStemFilterFactory"/>
    </analyzer>
  </fieldType>

  <!-- Edge n-gram for autocomplete / prefix search -->
  <fieldType name="text_suggest" class="solr.TextField" positionIncrementGap="100">
    <analyzer type="index">
      <tokenizer class="solr.StandardTokenizerFactory"/>
      <filter class="solr.LowerCaseFilterFactory"/>
      <filter class="solr.EdgeNGramFilterFactory" minGramSize="2" maxGramSize="20"/>
    </analyzer>
    <analyzer type="query">
      <tokenizer class="solr.StandardTokenizerFactory"/>
      <filter class="solr.LowerCaseFilterFactory"/>
    </analyzer>
  </fieldType>

  <!-- ── Fields ──────────────────────────────────────────────────────────── -->

  <field name="id"                    type="string"  required="true" indexed="true" stored="true"/>
  <field name="_version_"             type="long"    indexed="true"  stored="false"/>
  <field name="_root_"                type="string"  indexed="true"  stored="false" docValues="false"/>
  <field name="suggest"               type="text_suggest" indexed="true" stored="false" multiValued="true"/>

  <!-- Product core -->
  <field name="sku"                   type="long"    indexed="true"  stored="true"/>
  <field name="name"                  type="text_en" indexed="true"  stored="true"/>
  <field name="name_str"              type="string"  indexed="true"  stored="true"/>
  <field name="type"                  type="string"  indexed="true"  stored="true"/>
  <field name="manufacturer"          type="string"  indexed="true"  stored="true" docValues="true"/>
  <field name="modelNumber"           type="string"  indexed="true"  stored="true"/>
  <field name="description"           type="text_en" indexed="true"  stored="true"/>
  <field name="shortDescription"      type="text_en" indexed="true"  stored="true"/>
  <field name="longDescription"       type="text_en" indexed="true"  stored="true"/>

  <!-- Taxonomy -->
  <field name="categoryPath"          type="strings" indexed="true"  stored="true"/>
  <field name="categoryLeaf"          type="string"  indexed="true"  stored="true" docValues="true"/>
  <field name="departmentId"          type="string"  indexed="true"  stored="true" docValues="true"/>

  <!-- Pricing -->
  <field name="regularPrice"          type="float"   indexed="true"  stored="true"/>
  <field name="salePrice"             type="float"   indexed="true"  stored="true"/>
  <field name="onSale"                type="boolean" indexed="true"  stored="true"/>

  <!-- Ratings -->
  <field name="customerReviewAverage" type="float"   indexed="true"  stored="true"/>
  <field name="customerReviewCount"   type="int"     indexed="true"  stored="true"/>

  <!-- Availability -->
  <field name="onlineAvailability"         type="boolean" indexed="true" stored="true"/>
  <field name="inStoreAvailability"        type="boolean" indexed="true" stored="true"/>
  <field name="availableForInStorePickup"  type="boolean" indexed="true" stored="true"/>
  <!-- Per-store availability resolved at query time via Redis bitmaps -->
  <field name="storeAvailable"        type="boolean" indexed="false" stored="false"/>

  <!-- Media -->
  <field name="thumbnailImage"        type="string"  indexed="false" stored="true"/>
  <field name="image"                 type="string"  indexed="false" stored="true"/>
  <field name="url"                   type="string"  indexed="false" stored="true"/>

  <!-- ── Copy fields ──────────────────────────────────────────────────────── -->
  <copyField source="name"         dest="suggest"/>
  <copyField source="manufacturer" dest="suggest"/>
  <copyField source="name"         dest="name_str"/>

  <!-- ── Unique key ───────────────────────────────────────────────────────── -->
  <uniqueKey>id</uniqueKey>

</schema>
